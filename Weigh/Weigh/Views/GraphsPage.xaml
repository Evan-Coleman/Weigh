<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             xmlns:chart="clr-namespace:Syncfusion.SfChart.XForms;assembly=Syncfusion.SfChart.XForms"
             xmlns:rangenavigator="clr-namespace:Syncfusion.RangeNavigator.XForms;assembly=Syncfusion.SfChart.XForms" 
             x:Class="Weigh.Views.GraphsPage"
             Title="{Binding Title}">

    <StackLayout Padding="10" Spacing="5">

        <chart:SfChart HeightRequest="250">

            <chart:SfChart.PrimaryAxis>

                <chart:DateTimeAxis RangePadding="Round">
                    <chart:DateTimeAxis.LabelStyle>
                        <chart:ChartAxisLabelStyle LabelFormat="MM/dd"/>
                    </chart:DateTimeAxis.LabelStyle>
                </chart:DateTimeAxis>

            </chart:SfChart.PrimaryAxis>

            <chart:SfChart.SecondaryAxis>

                <chart:NumericalAxis>

                    <chart:NumericalAxis.Title>

                        <chart:ChartAxisTitle Text="Weight"></chart:ChartAxisTitle>

                    </chart:NumericalAxis.Title>

                </chart:NumericalAxis>

            </chart:SfChart.SecondaryAxis>

            <chart:SfChart.Series>

                <chart:LineSeries ItemsSource="{Binding ChartData}" XBindingPath="WeighDate" YBindingPath="Weight"
                                  EnableDataPointSelection="False">
                    <chart:LineSeries.DataMarker>

                        <chart:ChartDataMarker>

                            <chart:ChartDataMarker.LabelStyle>

                                <chart:DataMarkerLabelStyle/>

                            </chart:ChartDataMarker.LabelStyle>

                        </chart:ChartDataMarker>

                    </chart:LineSeries.DataMarker>
                </chart:LineSeries>

            </chart:SfChart.Series>

        </chart:SfChart>

        <ListView ItemsSource="{Binding WeightList}"
                  VerticalOptions="FillAndExpand"
                  HasUnevenRows="True"
                  x:Name="bob">
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                   

                                <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Path=Weight, StringFormat='Weight: {0}'}"
                                           HorizontalOptions="StartAndExpand"/>
                            <Label Text="{Binding Path=WeightDelta, StringFormat='Change from last entry: {0}'}"
                                           HorizontalOptions="StartAndExpand"/>
                            <Label Text="{Binding Path=WeighDate, StringFormat='Date: {0:MM/dd/yyyy hh:mm}'}"
                                           HorizontalOptions="EndAndExpand"/>
                                </StackLayout>
  
           




                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>

</ContentPage>